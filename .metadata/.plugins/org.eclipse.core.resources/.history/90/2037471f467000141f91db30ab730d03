/**
 * 
 */

/**
 * @author maddierook
 *
 */
public class RecursionTest {

	/**
	 * A method that calls the recursive function to print an integer list.
	 * 
	 * @param args
	 */
	public static void main(String[] args) {
		
		int min = 1;
		int max = 10;
		
		printIntegers(min, max);

		System.out.println();

		printSum(min, max);

	}
	
	
	/**
	 * A method to recursively print the integers min to max.
	 * 
	 * @param min The starting integer.
	 * @param max The ending integer.
	 */
	public static void printIntegers(int min, int max) {
		
		if (min < max) {
			System.out.print(min + ", ");
			printIntegers(min+1, max);
		} else {
			System.out.println(max);
		}
				
	}
	
	/**
	 * A method to print out the sum of integers min to max.
	 * 
	 * @param min The starting integer.
	 * @param max The ending integer.
	 * @return
	 */
	public static int printSum(int min, int max) {
		
		
		if (min < 10) {
			System.out.println(min);
			return printSum(min+1, max);
		} else {
			System.out.println(max);
			return max;
		}
		
	}
	
}
